# 生工数据

## 生成的数据结构

| 文件                | 康为规则                            | 生工规则             |
| ----------------- | ------------------------------- | ---------------- |
| ab1_file文件        | `.ab1`后缀                        | `.ab1`后缀         |
| bam文件             | `.bam`后缀                        | `.bam`后缀         |
| consensus_fasta文件 | `.consensus.fasta`后缀            | `.assembly.fa`后缀 |
| coverage_png文件    | `.coverage.png`后缀               | `.depth.pdf`后缀   |
| fastq文件           | `.fastq`/`.fastq.gz`/`.fq.gz`后缀 | `.fastq.gz`后缀    |
| length_peak_png文件 | `_read_length_peak.png`后缀       | `.readlen.pdf`后缀 |

## 前处理

### 1.准备文件
```powershell
# 获取目录下文件
Get-ChildItem
# 批量改文件名
Get-ChildItem * | Rename-Item -NewName { $_.Name -replace "71116542421", "DLE1800073-1" } -WhatIf
```

### 2.xlsx文件支持

packages.datasplit_v3.strain_identification.strain_identification_third.var_judge.py
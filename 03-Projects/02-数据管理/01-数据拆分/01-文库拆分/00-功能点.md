- [x] 数据库中的信息导出为json文件
	- [x] illumina数据(bcl文件)
		- [ ] 获取create_fastq_for_index_reads参数(在lane中因为历史原因为string型, 0 - 不生成index, 1 - 保留index(ATAC文库), 2 - 保留index(I1,I2) )
		- [ ] 获取sample_index参数(文库层面, 0 - 不生成index, 1 - 保留index(ATAC文库), 2 - 保留index(I1, I2))
		- [x] 生产sample_sheet.csv文件(默认都输出index，后续进行删除)
	- [x] T7数据(ztron已经拆分完的fastq.gz数据)
- [x] illumina数据
	- [x] bcl-convert拆分
		- [x] 支持ATAC文库(R1,R2,R3三个fastq文件的生产)
		- [x] 支持特殊单细胞(R1,R2,I1,I2四个fastq文件的生产)
	- [-] bcl2fastq拆分(不必再支持)
	- [x] 拆分完日志解析获得总体的信息
	- [x] 拆分完成获取拆分的文件大小
	- [-] 拆不出数据特殊文库的处理(bcl-convert如果拆不出数据会自己生成空文件) 
	- [ ]  并行处理单个文库
		- [?] 某些文库进行tile过滤
		- [x] 某些文库进行clumpify压缩
			- [x] 根据product_type(拆分类型)和product_id_id_id(三级产品编号)判断
			- [x] 文件大小判断(r1端fastq>5G的文件不处理)
		- [-] 检查fastq的id是否正确,为了解决Xplus测序仪的id重复bug(但测序仪经过升级以后不会有这个现象)
		- [x] 使用seqkit获取reads和fastq的统计，同时替代gzip进行完整性检查
	- [x] 拆分完根据规则重命名文件
	 - [x] 画tile图
- [x] T7拆分 
	- [x] 判断文件存不存在
	- [-] 进行部分数据截取
	- [-] 进行md5检查(拆分出来的数据已经不带md5文件了)
	- [x] 获取reads和bases数
	- [x] 获取ztron拆分汇总文件
- [x] 结果整理(共用一个tool)
- [-] 上传之前检查fastq.gz文件是否损坏(IO问题导致)
	- [-] fastq格式检查
	- [-] gz文件检查
- [ ] 导入数据库
	- [ ] tile过滤信息(fastqc.stat和stat.txt文件)
	- [ ] 单个文库的原始文件大小信息(stat_size.json文件, sg_split_specimen表的origin_bytes字段)
	- [ ] 单个样本的原始文件大小信息(stat_size.json文件, sg_split_specimen表的raw_origin_bytes字段)
	- [ ] 上传的总文件大小(total_size.json文件, sg_split表中split_size.library_split)




- [x] 将数据库信息导出为json文件
- [x] 生产sample_sheet.csv文件
- [/] 拆分完日志解析（正在处理）
- [>] 画tile图（等有空再做）
- [-] 拆不出数据特殊文库的处理（已放弃）
- [x] 上次之前检查fastq.gz文件是否损坏（磁盘IO报错）
- [?] 某些文库进行tile过滤（需求待确认）
- [*] 并行处理单个文库（核心任务）
- [~] 文件大小判断（低优先级）
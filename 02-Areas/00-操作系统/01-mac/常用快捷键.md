# 常用快捷键

## 操作应用

- `Command + M`: 最小化当前窗口到 Dock
- `Command + Option + M`: 最小化当前应用的所有窗口
- `Command + H`: 隐藏当前应用（不是最小化，窗口从 Dock 消失，更干净）
- `Command + Option + H`: 隐藏其他所有应用，只保留当前窗口‘
- `Command + ~`: 同一应用内窗口循环切换（最常用）
- `Command + Shift + ~`: 反向循环切换